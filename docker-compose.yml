services:
  voygo-web:
    image: nginx:alpine
    container_name: voygo-web
    ports:
      - "8080:80"
    volumes:
      - ./voygo:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
